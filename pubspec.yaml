name: test_for_rubium
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_bloc: ^8.0.1
  shared_preferences: ^2.0.13
  http: ^0.13.4
  dartz: ^0.10.1
  flutter_map: ^0.14.0
  latlong2: ^0.8.1
  geolocator: ^8.0.5

flutter:
  uses-material-design: true
